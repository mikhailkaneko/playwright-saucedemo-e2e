{
  "name": "playwright-saucedemo-e2e",
  "version": "1.0.0",
  "description": "End-to-end test automation suite for SauceDemo built with Playwright and TypeScript, using the Page Object Model.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
